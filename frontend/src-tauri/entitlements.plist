<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Hardened Runtime entitlements for IMSLP Accompanist.

      JVM (bundled Adoptium 25 under runtime/jre) requires the JIT
      entitlement at minimum. allow-unsigned-executable-memory covers
      JNI helper paths the JVM exercises that strict W^X rejects.

      disable-library-validation is required because the bundled
      tree includes dylibs (libpoppler.X.dylib, libleptonica, JVM
      internals, PyInstaller's _MEIxxxxx tree) signed with our
      Developer ID but loaded by binaries Apple did not sign.

      allow-dyld-environment-variables lets main.rs propagate
      JAVA_HOME / TESSDATA_PREFIX / PATH overrides to the sidecar
      without the loader stripping them under Hardened Runtime.
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
